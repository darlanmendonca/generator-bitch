<!DOCTYPE html>
<html<% if ((appType === 'client' || appType === 'both') && appFramework === 'angular') { %>(ng-app='<%= slugify(appname) %>')<% } %>>
<head>
  <meta charset="UTF-8">
  <title>Document</title>
  <base href="/">
  <%%- include includes/meta %%>
  <%%- include includes/favicons %%>


  <%%- include includes/styles %%>
</head>
<body><% if ((appType === 'client' || appType === 'both') && appFramework === 'angular') { %>
  <div ui-view></div><% } else { %>
  <p>hello</p><% } %>

  <%%- include includes/scripts %%>
</body>
</html>
